<%- include("partials/header") %>

<h1>Blog</h1>

<% blogs.forEach((blog) => { %>

<div>
    <h2> <%= blog.title %> </h2>
    <img src="<%= blog.image %>">
</div>

<span><%= blog.created %></span>
<p><%= blog.body %></p>

<% }) %>

<%- include("partials/footer") %>